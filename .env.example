# API base URL (client-side). Use /api/v1 to proxy via Next.js rewrites.
NEXT_PUBLIC_API_URL=/api/v1

# NestJS backend URL for Next.js proxy rewrites
API_PROXY_URL=http://localhost:3001

# Default family ID for creating user-admin (POST /user-admin)
NEXT_PUBLIC_DEFAULT_FAMILY_ID=family-uuid-1111
